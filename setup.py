from setuptools import setup, find_packages

setup(
    name='v2ray_proto_python',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)
